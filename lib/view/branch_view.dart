import 'package:flutter/material.dart';

class BranchView extends StatelessWidget {
  const BranchView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Branch View'),
    );
  }
}
